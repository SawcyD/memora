# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Keep the reusable UI package installable when copied directly. Its compiled
# distribution is intentional; only the disposable showcase build is ignored.
!packages/memora-ui/dist/
!packages/memora-ui/dist/**
packages/memora-ui/demo-dist/
src-tauri/tauri.release.generated.conf.json

# npm pack output — regenerate with `npm run ui:pack`, never commit
/*.tgz

# keys
*.key
*.pem

# Scratch output (icon generation intermediates, etc.)
tmp

# Agent configuration (kept local)
.claude
AGENTS.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
